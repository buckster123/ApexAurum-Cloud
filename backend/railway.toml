[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Temporarily disabled healthcheck for debugging
# healthcheckPath = "/health"
# healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
